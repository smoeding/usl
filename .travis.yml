language: r
sudo: required
cache: packages
r:
  - oldrel
  - release
  - devel
# r_build_args: "--no-build-vignettes --no-manual"
# r_check_args: "--no-build-vignettes --no-manual --as-cran"

warnings_are_errors: false

before_install:
  - tlmgr install mathdesign bera units
